

const API_CONFIG = {
  OPEN_WEATHER: {
    KEY: "", // ← Pasted from OpenWeatherMap API key
    BASE_URL: "https://api.openweathermap.org/data/2.5",
    UNITS: "metric", // Celsius
    EXCLUDE: "minutely,hourly", // Only need daily
    LANGUAGE: "en"
  }
};
